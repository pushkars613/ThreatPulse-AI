# Railpack copies ONLY this file into the pip install layer, so it cannot use
# "-r backend/requirements.txt" -- that path does not exist at install time.
# Must stay in sync with backend/requirements.txt; enforced by
# backend/tests/test_deployment.py::test_root_requirements_match_backend
fastapi>=0.115,<1
openai>=1.58,<3
pydantic>=2.10,<3
pydantic-settings>=2.7,<3
python-dotenv>=1,<2
python-multipart>=0.0.20,<1
uvicorn[standard]>=0.34,<1
pandas
requests
